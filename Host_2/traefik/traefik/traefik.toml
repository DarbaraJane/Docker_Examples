[providers.docker]
  endpoint = "unix:///var/run/docker.sock"
  network = "proxy"
  watch = true

[providers.file]
  directory = "/etc/traefik/dynamic"
  watch = true
[entryPoints]
  [entryPoints.web]
    address = ":80"
      [entryPoints.web.http.redirections.entryPoint]
        to = "websecure"
        scheme = "https"
        permanent = true

  [entryPoints.websecure]
    address = ":443"
    [entryPoints.websecure.http.tls]

[api]
  #insecure = true
  dashboard = true

[certificatesResolvers.NWCSB.acme]
  email = "darby.fahringer@nwcsb.com"
  storage = "/letsencrypt/acme.json"
  [certificatesResolvers.NWCSB.acme.dnsChallenge]
    provider = "cloudflare"
    resolvers = ["1.1.1.1:53", "8.8.8.8:53"]

[log]
  level = "DEBUG"
  format = "common"